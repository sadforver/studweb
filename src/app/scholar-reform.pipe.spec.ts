import { ScholarReformPipe } from './scholar-reform.pipe';

describe('ScholarReformPipe', () => {
  it('create an instance', () => {
    const pipe = new ScholarReformPipe();
    expect(pipe).toBeTruthy();
  });
});
